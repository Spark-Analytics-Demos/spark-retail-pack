# =============================================================================
# spark_retail_pack — Python Dependencies
# =============================================================================
# Pinned per Section 4 Part 3 §4.48 (canonical list).
# Install with: pip install -r requirements.txt
#
# Recommended: use a virtual environment
#   python -m venv .venv
#   .venv\Scripts\activate         # Windows
#   source .venv/bin/activate      # macOS/Linux
#   pip install -r requirements.txt
# =============================================================================

# Core dbt
# dbt-core 1.11.x required for Python 3.13 (1.9 has no 3.13 wheel). Per ADR-005.
dbt-core==1.11.11
dbt-snowflake==1.9.4

# MetricFlow — semantic layer engine. Used OFFLINE to generate Path 2
# semantic views (scripts/generate_semantic_views.py); NOT required at
# dbt build / CI time. Per ADR-005. Pulls metricflow==0.211.0.
dbt-metricflow==0.13.0

# SQL linting (used in CI, optional for local dev)
sqlfluff==3.0.7
sqlfluff-templater-dbt==3.0.7

# Pre-commit hooks (optional local dev tool)
pre-commit==3.7.0
